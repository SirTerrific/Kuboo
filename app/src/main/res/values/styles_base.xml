<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- SplashThemeBase and BaseThemeBase carry the actual styling. The themes the
         manifest points at are thin wrappers so that values-v35 can add the
         edge-to-edge opt-out without repeating every item. -->
    <style name="SplashThemeBase" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/app_splash_background</item>
    </style>

    <style name="SplashTheme" parent="SplashThemeBase" />

    <style name="BaseThemeBase" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:buttonStyle">@style/OpenSansButtonStyle</item>
        <item name="android:textViewStyle">@style/OpenSansTextViewStyle</item>
        <item name="android:windowContentTransitions">true</item>
        <item name="preferenceTheme">@style/PreferenceTheme</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="BaseTheme" parent="BaseThemeBase" />

    <style name="PreferenceTheme" parent="PreferenceThemeOverlay">
        <item name="android:textViewStyle">@style/OpenSansTextViewStyle</item>
    </style>

    <style name="OpenSansTextViewStyle" parent="android:Widget.TextView">
        <item name="android:fontFamily">@font/opensans_regular</item>
    </style>

    <style name="OpenSansButtonStyle" parent="android:Widget.Holo.Button">
        <item name="android:fontFamily">@font/opensans_regular</item>
    </style>

    <style name="ErrorTextStyle" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/md_red_500</item>
        <item name="android:textSize">13sp</item>
    </style>

    <style name="HintTextStyle" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/md_teal_500</item>
        <item name="android:textSize">12sp</item>
    </style>

</resources>